# Core dependencies for NPU-accelerated LLM inference
numpy>=1.24.0
openvino>=2024.0.0
openvino-tokenizers>=2024.0.0
onnxruntime>=1.17.0
transformers>=4.36.0
torch>=2.1.0
optimum[openvino]>=1.16.0

# Utilities
pyyaml>=6.0
psutil>=5.9.0
tqdm>=4.65.0

# Optional: For GPU fallback
# onnxruntime-gpu>=1.17.0

# Development dependencies (install separately with -r requirements-dev.txt)
